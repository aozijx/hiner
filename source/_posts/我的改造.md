---
title: 我的改造
date: 2025-03-16 18:09:10
author: Hiner
categories: [Hexo, solitude]
tags: 
cover: img/default.avif
---
## 右键菜单添加复制当前页面链接按钮
新建 `/themes/solitude/js/copylink.js` ，内容如下：

```JavaScript
function copyPostLink() {
    const postLink = window.location.href;

    navigator.clipboard.writeText(postLink).then(() => {
        utils.snackbarShow("当前页面链接已复制到剪贴板", false, 2000);
    }).catch(err => {
        console.error("无法复制链接", err);
        utils.snackbarShow("复制失败，请重试", false, 2000);
    });
}
```
修改 `_config.solitude.yml` ，引入js：

```yaml
extends:
   body:
       - <script src="/js/copylink.js" async></script>
```
然后右键菜单添加该项即可：
```yaml
right_menu:
  custom_list:
     - name: 复制链接
       click: copyPostLink()
       id: menu-copyPostLink
       class:
       icon: fas fa-link
```
第六项：https://r1n.top/post/c28e60f4.html

## 导航栏页面
{% link 'solitude' '官方文档' 'https://solitude.js.org/feature/links.html' %}

## links 页面
想改成只显示 title ，hover 显示 desc，调不好，只能这样了，当 avatar 值为 None 触发，但就在我要推送的时候，我想到：为什么不把条件设置为空呢😅🤣
- card
> themes\solitude\layout\includes\widgets\page\links\linksCard.pug >line 14
```pug
if item.avatar
    .site-card-avatar
        img.flink-avatar.cf-friends-avatar(src=item.avatar + (data.suffix || ''), alt=item.name)
        //- .img-alt.is-center= item.name
else    
    - var avatarStyle =  'display:none'
.site-card-text
    span.title.cf-friends-name= item.name
    span.desc(title=item.descr)= item.descr
```

- banner
> themes\solitude\layout\includes\widgets\page\links\banner.pug >line 30
```pug
a.tags-group-icon(href=url_for(y.link), title=y.name)
    if y.avatar
        img(src=y.avatar + (site.data.links.banner_suffix || ''), title=y.name)
    else
        img(src=y.topimg + (site.data.links.banner_suffix || ''), title=y.name)
    span.tags-group-title=y.name
```
> themes\solitude\source\css\_page\links.stylline >line 152 .site-card-text > .title
```styl
padding-bottom 7px
```

## about 页面
- 游戏 icon 显示异常
> themes\solitude\layout\includes\widgets\page\about\hobbies.pug >line 16
```pug
.icon-group
    each icon in game.icon_group
        i(style=`background: url(${icon}) center / cover;`)
```

- tenyear
发现了一个没有使用的模板，问 AI 然后配置了一下
```yaml
tenyear:
  title: 十年
  tips: 测试
  text: 
  start: 2025-04-07
  end: 2035-04-07
```